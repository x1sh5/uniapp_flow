/* 1. 居中灰色分割线 
.divider {
  width: 100%;
  height: 2rpx;
  background-color:#6c4ad1;
  margin-top: 20rpx; 
}
.vertical-line {
  position: absolute;
  margin-top: 2rpx;
  left: 50%; 
  transform: translateX(-50%); 将线居中
  height: 600rpx; 设置线的高度 
  width:2rpx;
  background-color: #6c4ad1;
}
*/
.horizontal-line-0 {
  position: absolute; /* 启用绝对定位 */
  z-index: 2;
  transform: translate(0rpx,0rpx) scale(1);
  width: 800rpx;
  height: 80rpx; /* 设置线的厚度 */
  background-color: #6c4ad1;
  margin-top: 0rpx; /* 控制线与上方内容的距离 */
  background-image: -webkit-linear-gradient(0deg, #886cdb 0%, #886cdb 100%);
  background-color: #acd126;
}
.horizontal-line {
  position: absolute; /* 启用绝对定位 */
  z-index: 2;
  transform: translate(230rpx,0rpx) scale(1);
  width: 30rpx;
  height: 310rpx; /* 设置线的厚度 */
  background-color: #6c4ad1;
  margin-top: 0rpx; /* 控制线与上方内容的距离 */
  background-image: -webkit-linear-gradient(90deg, #886cdb 0%, #886cdb 100%);
}
.container {
	position: absolute; /* 启用绝对定位 */
	display: flex;
	justify-content: center;  /* 水平居中 */
	align-items: center; /* 垂直居中 */
	
	 transform: translate(20rpx, 150rpx) scale(1);
  background-size:contain; 
  	background-repeat: no-repeat; /* 禁止背景平铺 *//* 根据需要设置背景大小 */
  /* 其他背景样式属性，如背景颜色、位置等 */

  width: 700rpx;
  height: 700rpx;
  z-index: -5;

/* background-color: #acd126; 修改背景颜色为紫色  */
}
.checkbox-container {
  position: absolute; /* 启用绝对定位 */
  display: flex;
  justify-content: center;  /* 水平居中 */
  align-items: center; /* 垂直居中 */

  transform: translate(110rpx, 350rpx) scale(1.2);

  width: 500rpx;
  height: 40rpx;

  flex-direction: row; 
  /* background-color: #acd126; 修改背景颜色为紫色 */
  /* border: 1px solid transparent; 修改边框为透明 */
  margin-top: 0rpx;
}
.horizontal-line-a {
  position: absolute; /* 启用绝对定位 */
  z-index: 2;
  transform: translate(230rpx,430rpx) scale(1);
  width: 30rpx;
  height: 150rpx; 
  background-color: #6c4ad1;
  margin-top: 0rpx; /* 控制线与上方内容的距离 */
  background-image: -webkit-linear-gradient(90deg, #4d1ae4 0%, #886cdb 100%);
}
.horizontal-line-b {
  position: absolute; /* 启用绝对定位 */
  z-index: 2;
  transform: translate(230rpx,550rpx) scale(1);
  width: 160rpx;
  height: 30rpx; /* 设置线的厚度 */
  background-color: #6c4ad1;
  margin-top: 20rpx; /* 控制线与上方内容的距离 */
  background-image: -webkit-linear-gradient(0deg, #4d1ae4 0%, #886cdb 100%);
  background-color: #acd126;
}
.horizontal-line-c {
  position: absolute; 
  z-index: 2;
  transform: translate(610rpx,550rpx) scale(1);
  width: 60rpx;
  height: 30rpx; /* 设置线的厚度 */
  background-color: #6c4ad1;
  margin-top: 20rpx; /* 控制线与上方内容的距离 */
  background-image: -webkit-linear-gradient(0deg, #886cdb 0%, #4d1ae4 60%);
}
.horizontal-line-d {
  position: absolute; 
  z-index: 2;
  transform: translate(640rpx,580rpx) scale(1);
  width: 30rpx;
  height: 680rpx; /* 设置线的厚度 */
  background-color: #6c4ad1;
  margin-top: 20rpx; /* 控制线与上方内容的距离 */
  background-image: -webkit-linear-gradient(90deg, #886cdb 0%, #4d1ae4 100%);
}
.select-container{
  transform: translate(370rpx,470rpx) scale(0.8);
}
/* 2. 改变“请选择创建类型”字体颜色为灰色、大小30px，页面居中，上边距70px */
.title {
  z-index: 3;
  font-size: 30rpx;
  color: rgb(0, 0, 0);
  text-align: center;
  margin-top: 10px;
  text-shadow: 10px 5px 5px rgba(10, 0, 0,0.5);
}
.title1 {
  z-index: 3;
  transform: translate(36rpx,460rpx) scale(1);
  float: left;
  text-align: left;
  padding-left: 30rpx; 
  font-size: 24rpx;
  color: #9e81d1;
  margin-top: 60rpx;
  font-weight: bold;
  text-decoration: underline;

  /* text-shadow: 2rpx 2rpx 0rpx rgba(77, 29, 209, 1); 设置描边效果 */
}
.title2 {
  z-index: 3;
  transform: translate(-310rpx,80rpx) scale(1);
  float: right;
  text-align: right;
  padding-right: 30rpx; 
  font-size: 24rpx;
  color: #9e81d1;
  margin-top: 60rpx;
  font-weight: bold;
  text-decoration: underline;
}
/**/
/* 修改复选框内部样式 */
/* 5. “创建”按钮样式 */
.create-button{
  transform: translate(0rpx,420rpx) scale(1);

  border: 10px solid  #6c4ad1;
  border-radius: 0rpx; /*设置按钮边框为圆角 */
  border: none;
  width: 800rpx;
  height: 150rpx;
  color: rgb(255, 255, 255); 
  background-color: #4d1ae4; 
  font-size: 60rpx;
  text-align: center;
/* top: calc(150vh - 350rpx);
   */
 
 display: flex;  /* 使用Flex布局 */ 
   justify-content: center;  /* 水平居中 */
   align-items: center; /* 垂直居中 */
   margin-top: 700rpx;
   box-shadow:0ch;
 
   box-shadow: 5px 5px 5px rgba(116, 116, 116, 0.5);
}
